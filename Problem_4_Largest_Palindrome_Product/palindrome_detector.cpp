







/*
Ask user to enter number
  Use mechanism to split number into separate digits
  Perform palindrome check with adjusted conditions for even/odd number of indexes
    n[0] == n[size - 1], n[1] == n[size - 2], etc...
  If valid, cout it
  If not, continue
*/
